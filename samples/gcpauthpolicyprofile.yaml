apiVersion: gcpauthpolicy.nuxeo.io/v1alpha1
kind: GCPAuthPolicyProfile
metadata:
  name: pfouh
spec:
  features:
    imagePullSecrets:
      enabled: true
  datasource:
    secret:
      namespace: policies
      name: pfouh
